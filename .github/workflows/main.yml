name: Build & Release APK

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.19.0"
          channel: "stable"

      - name: Create Flutter project structure
        run: |
          # Backup our code and config
          mv lib lib_bak
          mv pubspec.yaml pubspec_bak

          # Initialize standard Flutter structure (generates android, ios, etc.)
          flutter config --no-analytics
          flutter create --template=app --project-name=fridge_feast --overwrite .

          # Restore our code and config
          rm -rf lib
          mv lib_bak lib
          mv pubspec_bak pubspec.yaml

          # Inject AdMob App ID into AndroidManifest.xml
          AD_MANIFEST="android/app/src/main/AndroidManifest.xml"
          if [ -f "$AD_MANIFEST" ]; then
            sed -i '/<application/a \        <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="ca-app-pub-7036399347927896~5750385934"/>' "$AD_MANIFEST"
          fi

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
